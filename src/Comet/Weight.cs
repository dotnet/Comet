using System;
namespace Comet
{
    /// <summary>
    /// A weight to use for fonts.
    /// </summary>
    public enum Weight
    {
        Regular = 400,
        Thin = 100,
        Ultralight = 200,
        Light = 300,
        Medium = 500,
        Semibold = 600,
        Bold = 700,
        Heavy = 800,
        Black = 900,
    }
}
